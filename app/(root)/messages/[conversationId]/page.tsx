async function Page({ params }: { params: { conversationId: string } }) {
  // const user = await currentUser();
  // if (!user) return null;

  //   const userInfo = await fetchConversation(params.conversationId);

  return (
    <section className="h-screen w-full bg-green-800 flex flex-col justify-center items-center"></section>
  );
}
export default Page;
